# Core runtime for benchmark on Torch 2.8 / CUDA 12.8.
# Recommended install flow:
#   1) install torch/torchaudio/torchvision 2.8.x for your CUDA build first
#   2) pip install -r requirements.txt

# LLM stack
transformers==4.56.0
vllm==0.11.0
ray==2.48.0

# HF and model utilities
accelerate==1.8.1
datasets==3.6.0
safetensors==0.5.3
tokenizers==0.22.0
peft==0.15.2

# Data and metrics
numpy==1.26.4
pandas==2.2.3
pyarrow==18.1.0
scikit-learn==1.7.2
bert-score==0.3.13

# API clients used by benchmarks/api
openai==1.67.0
anthropic==0.75.0
vertexai==1.71.1

# CLI / UX helpers
rich==13.9.4
tqdm==4.67.1
pyfiglet==1.0.4
pylatexenc==2.10
